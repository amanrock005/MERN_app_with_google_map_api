import User from "../models/user.model.js";

export const manualAddressUpdate = async (req, res) => {
  const { house, street, city, state, country, pincode } = req.body;
  try {
  } catch (err) {}
};
